# Extra dependencies for TTS_ENGINE=kokoro (see Dockerfile.kokoro).
# sherpa-onnx runs Kokoro-82M on onnxruntime — one self-contained wheel with no
# torch, transformers or spaCy behind it. Kept out of the default
# requirements.txt anyway so the base image carries no TTS model machinery.
-r requirements.txt
sherpa-onnx==1.13.5
soundfile==0.14.0
